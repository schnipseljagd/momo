#!/bin/bash
set -euo pipefail && cd "$(dirname "${BASH_SOURCE[0]}")"

# list packages by installation date from oldest to latest
pacman -Qen | expac --timefmt='%F %T' '%n %v %l' - | sort -k3 | awk '{ print $1 }'
